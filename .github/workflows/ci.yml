name: CI

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  validate:
    name: Lint, type-check, and build
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
      NEXT_PUBLIC_APP_URL: ${{ secrets.NEXT_PUBLIC_APP_URL }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint

      - name: Run type check
        run: npm run type-check

      - name: Build application
        run: npm run build

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Start application
        run: |
          npm run start > app.log 2>&1 &
          echo $! > app.pid

      - name: Wait for application
        run: npx wait-on http://127.0.0.1:3000

      - name: Run login smoke test
        run: npx playwright test tests/smoke.spec.ts

      - name: Stop application
        if: always()
        run: |
          if [ -f app.pid ]; then kill $(cat app.pid) || true; fi

      # Playwright smoke/e2e tests are intentionally left commented out for now.
      # When your test suite is ready, you can enable either approach below.
      #
      # Option A: run Playwright directly in the GitHub Actions runner.
      # - name: Install Playwright browsers
      #   run: npx playwright install --with-deps
      #
      # - name: Start application
      #   run: |
      #     npm run dev > app.log 2>&1 &
      #     echo $! > app.pid
      #
      # - name: Wait for application
      #   run: npx wait-on http://127.0.0.1:3000
      #
      # - name: Run Playwright smoke tests
      #   run: npx playwright test
      #
      # - name: Stop application
      #   if: always()
      #   run: |
      #     if [ -f app.pid ]; then kill $(cat app.pid) || true; fi
      #
      # Option B: run Playwright in a Docker container for a more reproducible environment.
      # - name: Run Playwright in Docker
      #   run: |
      #     docker run --rm \
      #       -v "$PWD:/work" \
      #       -w /work \
      #       mcr.microsoft.com/playwright:v1.60.0-jammy \
      #       bash -lc "npm ci && npx playwright test"
